# Core pipeline
transformers>=4.40.0
torch>=2.2.0
chromadb>=0.5.0
requests>=2.31.0
python-dotenv>=1.0.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Optional — LLM client (OpenRouter)
openrouter>=0.1.0

# Negation detection (medSpaCy ConText)
spacy>=3.7.0
medspacy>=1.0.0

# PHI scrubbing (presidio) — install if using presidio backend
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0
