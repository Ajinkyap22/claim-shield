{
  "name": "healthcare-claim-shield",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "frontend": "turbo run dev --filter=frontend"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18"
  }
}
